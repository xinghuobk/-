# ParaJudge v0.3 依赖（沙箱外）
# 安装：pip install -r requirements.txt

# 必要依赖（必装）
numpy>=1.24,<3.0
scipy>=1.10,<2.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<2.0
networkx>=3.0,<4.0
requests>=2.28
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0

# 可选依赖（按需）
sentence-transformers>=2.2     # T2 真实 embedding
torch>=2.0                     # sentence-transformers 后端
huggingface-hub>=0.16          # 下载 HF 数据集
openai>=1.0                    # OpenAI provider
dashscope>=1.14                # 阿里通义千问
ollama>=0.1.0                  # Ollama Python SDK（可选）
pyinstaller>=6.0               # 桌面端打包
pywebview>=5.0                 # 桌面端 UI
matplotlib>=3.5                # 论文图表
seaborn>=0.12                  # 统计图

# 开发
pytest>=7.0
black>=23.0
ruff>=0.1.0
