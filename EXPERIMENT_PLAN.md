# ParaJudge 系统迭代报告 - 漏洞检测与待实验论证清单

## 一、已修复漏洞汇总

### 1. 编码漏洞（已修复）
| ID | 位置 | 问题描述 | 修复方案 |
|----|------|----------|----------|
| V1 | `judgment_config.py` | 重复导入 `dataclass` | 删除第 68 行重复导入 |
| V2 | `prompts.py` | `build_judge_prompt_v2` 用 `.get()` 访问 Pydantic 模型 | 新增 `_get_field()` 兼容 dict/Pydantic |
| V3 | `llm_client.py` | Mock provider 返回 v1 格式，v2 模式缺 fact/value 字段 | 检测 v2 关键词并追加字段 |
| V4 | `schemas.py` | `FactChecker` 缺少 schema 定义 | 新增 `FactClaim`/`FactCheckReport` |
| V5 | `orchestrator.py` | `render_markdown` 未输出事实核查报告 | 新增 fact_check 报告节 |

### 2. 设计漏洞（已修复）
| ID | 问题 | 修复 |
|----|------|------|
| D1 | Mock 评分系统性偏向正方 | 改为对称 base(70/70) + hash 随机扰动 |
| D2 | T3 早停只建议不执行 | 在 `simple_debate.py` run() 循环中强制执行 |
| D3 | DS confidence 公式倒置 | 修正为 `agreement * (0.5 + 0.5 * gap)` |
| D4 | 反驳机制缺失 | 新增 `_check_rebuttal()` 强制回应对方论点 |

### 3. 理论漏洞（已标识）
| ID | 问题 | 状态 |
|----|------|------|
| T1 | DS 融合非真正 Dempster-Shafer | 已添加诚实标注 + 正交和近似实现 |
| T2 | 辩论胜负 ≠ 真理 | 需要外部 ground truth 验证 |
| T3 | 统计检验前提假设未验证 | 已添加 Shapiro-Wilk + Levene 检验 |

---

## 二、待实验论证清单

### P0 - 高优先级（需验证核心假设）

#### 1. 获胜阈值优化
- **假设**: 当前 WINNER_THRESHOLD=5.0 是否最优？
- **实验设计**:
  - 测试阈值: 2.0, 3.0, 5.0, 7.0, 10.0
  - 指标: 平局率、法官一致性、与 ground truth 相关性
- **预期结果**: 阈值过高 → 平局过多；阈值过低 → 误判率高

#### 2. 反驳检测有效性
- **假设**: 强制反驳检测能提升辩论质量
- **实验设计**:
  - 实验组: `REBUTTAL_REQUIRED=True`
  - 对照组: `REBUTTAL_REQUIRED=False`
  - 指标: 论点相关性、法官评分一致性、事实核查通过率
- **预期结果**: 开启反驳检测后，论点更聚焦，离题率下降

#### 3. DS 双路融合验证
- **假设**: DS 正交和近似优于启发式融合
- **实验设计**:
  - 对比三种融合方式: 简单平均 / 启发式 / DS 正交和
  - 指标: 与 ground truth 的 Spearman 相关性
- **预期结果**: DS 正交和在法官分歧大时表现更好

#### 4. T3 早停质量影响
- **假设**: 早停不影响裁决质量但节省算力
- **实验设计**:
  - 实验组: 启用 T3 早停
  - 对照组: 固定轮次
  - 指标: 裁决结果稳定性、计算耗时、与完整辩论的一致性
- **预期结果**: 早停能减少 30-50% 轮次，结果一致性 > 90%

### P1 - 中优先级

#### 5. 事实/价值区分效果
- **假设**: 区分事实与价值论证能提升裁决准确性
- **实验设计**:
  - 实验组: `use_judge_v2=True`
  - 对照组: `use_judge_v2=False`
  - 指标: 事实声明识别准确率、事实核查结果与评分的相关性
- **预期结果**: v2 能更好区分可验证事实与主观价值

#### 6. 惩罚机制有效性
- **假设**: Moderator 惩罚能有效约束违规行为
- **实验设计**:
  - 测试不同惩罚强度组合
  - 指标: 违规论点比例变化、裁决结果稳定性
- **预期结果**: 适度惩罚（当前配置）最优

#### 7. 正向消融基线
- **假设**: 各创新点(T1-T4)对最终结果有增量贡献
- **实验设计**:
  - 消融实验: 依次开启/关闭各组件
  - 指标: 与 ground truth 相关性变化
- **预期结果**: 组合使用效果优于单一组件

### P2 - 低优先级

#### 8. ICC(2,k) 修正
- **任务**: 将 ICC 计算修正为随机效应模型 ICC(2,k)
- **当前状态**: 使用简单一致性指标
- **验证方式**: 使用 IBM-ArgQ 数据集验证评分者信度

#### 9. 多模态证据融合
- **假设**: 融合多源证据能提升论证质量评估
- **实验设计**: 对比仅文本 vs 文本+结构化数据
- **当前障碍**: 缺乏结构化证据数据

---

## 三、数据集接入待办

| 数据集 | 状态 | 验证指标 |
|--------|------|----------|
| IBM-ArgQ-30k | Mock 就绪 | Spearman/Kendall 相关性 |
| FEVER | 待下载 | 事实核查准确率 |
| Perspectrum | 待下载 | 观点多样性评估 |
| CMV | 待下载 | 说服效果评估 |

---

## 四、待验证的核心假设

1. **辩论质量假设**: ParaJudge 的评分与人类专家评分高度相关
2. **创新点有效性**: T1-T4 组合使用 > 任意单一方法
3. **事实核查价值**: 事实核查结果能显著影响裁决
4. **一致性假设**: 同一问题多次运行结果稳定（seed 固定）
5. **公平性假设**: 系统不偏向任何一方（pro/con 对称）

---

## 五、实验验证路线图

```
阶段1: 基础验证（当前）
├─ Mock 数据验证通过
└─ 模块集成完成

阶段2: 真实数据验证（下一步）
├─ 接入 IBM-ArgQ-30k
├─ 验证核心假设 1-5
└─ 确定最优参数配置

阶段3: 消融实验
├─ 验证各创新点贡献
└─ 输出组件重要性排序

阶段4: 对比基准
├─ 与现有方法对比
└─ 发表级实验结果
```

---

## 六、风险清单

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| ground truth 数据集不可用 | 中 | 无法验证准确性 | 准备多套备选数据集 |
| LLM 输出不稳定 | 高 | 结果波动 | 使用 seed 固定 + 多次运行取平均 |
| 计算资源限制 | 中 | 无法大规模实验 | Mock 模式快速迭代 + 增量验证 |
| 评分主观性 | 高 | 难以量化评估 | 多法官平均 + ICC 信度指标 |
